/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Project/Maven2/JavaApp/src/main/java/${packagePath}/${mainClassName}.java to edit this template
 */

package victorgomez.calculadora;


/**
 *
 * @author DAM2
 */
//Clase usada para pruebas
public class Calculadora {

    public static void main(String[] args) {
        /*
        KeyListener teclado = new KeyListener() {
            @Override
            public void keyTyped(KeyEvent e) {
                
                System.out.println("Tecla "+e.getKeyCode());
            }

            @Override
            public void keyPressed(KeyEvent e) {
                
                System.out.println("Tecla "+e.getKeyCode());
            }

            @Override
            public void keyReleased(KeyEvent e) {
                
                System.out.println("Tecla "+e.getKeyCode());
            }
        };
        JFrame pantalla = new JFrame();
        pantalla.setSize(300, 300);
        pantalla.setVisible(true);
        
        pantalla.addComponentListener((ComponentListener) teclado);
        */
        
        }
}
